Fixer la syntaxe enum de Lease.opensignStatus : remplacer la syntaxe invalide { values: [...] } par enum: ['DRAFT', 'PENDING', 'SIGNED', 'EXPIRED', 'CANCELLED'] (format Mongoose valide). Vérifier que la validation fonctionne avec un test. Écrire un script de migration qui normalise les valeurs existantes en base.
Normaliser le casing de TOUS les enums dans les 11 modèles Mongoose : tout doit être en UPPER_SNAKE_CASE (MEUBLE, NU, MOBILITE, DPE_A, etc.). Lister chaque modèle, chaque champ enum, vérifier les incohérences de casse. Écrire un script de migration MongoDB qui met à jour les documents existants. Mettre à jour le code qui référence ces enums.
Ajouter les index manquants sur toutes les clés étrangères et champs fréquemment requêtés : Property.user, Candidature.property, Candidature.tenant, Application.property, Application.user, Lease.property, Lease.tenant, Document.application, Event.property, IdentitySession.user, Lead.email. Ajouter aussi les index composés pertinents (property + status).
Remplacer les 15+ champs Schema.Types.Mixed par des sous-schémas Mongoose typés. Créer des sous-schémas pour : aiAnalysis (documentType, confidence, extractedFields, flags), guarantee (type, guarantorId, amount), breakdown (loyer, charges, totalTTC). Appliquer sur Application, Document, Lease.
Ajouter les validations manquantes sur les champs numériques et dates : tous les montants >= 0 (min: 0), les dates de fin > dates de début (validation custom pre-save), format email avec regex dans User, format téléphone français dans les profils. Couvrir au moins les champs des modèles Property, Lease, Application, User.
Découper le schéma Application (50+ champs) en sous-documents logiques : créer un sous-schéma ApplicantProfile (nom, prénom, email, téléphone), ApplicantVerification (identityStatus, diditSessionId), ApplicantDocuments (sous-tableau typé), ApplicantScoring (score, flags, aiAnalysis). Garder la rétro-compatibilité.
Vérifier que tous les 11 modèles Mongoose ont { timestamps: true } activé dans les options du schéma. Ajouter createdAt et updatedAt là où c'est manquant. Écrire un script qui ajoute les timestamps aux documents existants qui n'en ont pas.
