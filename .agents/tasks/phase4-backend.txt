Ajouter la validation Zod sur toutes les API Routes de app/api/ : créer un helper validateRequest(schema, body) qui parse le body avec un schéma Zod et retourne 400 avec les erreurs formatées si invalide. Appliquer sur tous les endpoints POST/PUT/PATCH existants. Créer les schémas dans un dossier lib/validations/.
Implémenter un middleware d'erreur centralisé : dans server.js ajouter un error handler Express qui catch toutes les erreurs, log en JSON structuré (timestamp, method, path, statusCode, message), et retourne une réponse d'erreur sans stack trace en production. Pour les API Routes Next.js, créer un wrapper withErrorHandler qui fait la même chose.
Vérifier et corriger la logique du dépôt de garantie dans le code : dans Lease model, ajouter une validation pre-save qui vérifie que depositAmount === 0 si leaseType === 'MOBILITE', depositAmount <= rentAmount si leaseType === 'NU', depositAmount <= 2 * rentAmount si leaseType === 'MEUBLE'. Ajouter la même validation dans l'API de création/modification de bail. Écrire des tests unitaires.
Créer les endpoints RGPD manquants : GET /api/user/export qui retourne toutes les données de l'utilisateur en JSON (profil, candidatures, baux, documents metadata sans les fichiers), DELETE /api/user/data qui anonymise le compte et supprime les données personnelles, PUT /api/user/consent pour gérer le consentement Didit. Chaque endpoint doit vérifier l'authentification et logger l'action.
Auditer et corriger le cron rgpdPurge.js : vérifier qu'il supprime les candidatures refusées après 3 mois, les pièces d'identité des candidats non retenus immédiatement après décision, les données biométriques Didit après vérification, les leads marketing sans interaction depuis 3 ans. Ajouter des logs pour chaque suppression et un rapport de purge dans les logs.
