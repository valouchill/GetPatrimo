Créer un workflow GitHub Actions dans .github/workflows/ci.yml qui s'exécute sur chaque push et PR : installer les dépendances, lancer eslint, lancer tsc --noEmit (type-check), lancer npm test (tests unitaires). Le workflow doit utiliser Node.js 20, cacher node_modules, et bloquer le merge si un check échoue.
Améliorer le Dockerfile : ajouter un health check (HEALTHCHECK CMD curl -f http://localhost:3000/health || exit 1), utiliser un user non-root (USER node), implémenter un multi-stage build (stage build + stage production), optimiser le layer caching en copiant package.json avant le reste du code.
Écrire des tests d'API pour les 10 endpoints les plus critiques avec le test runner natif Node.js : POST /api/auth/register, POST /api/auth/login, GET /api/properties, POST /api/properties, GET /api/applications/:id, POST /api/applications, POST /api/leases, GET /api/user/profile, POST /api/documents/upload, POST /api/stripe/webhook. Chaque test vérifie le status code, le format de réponse, et les cas d'erreur (401, 400, 404).
Vérifier que le bail généré par BailInstant.tsx et LeaseWizard.tsx contient TOUTES les mentions obligatoires de la loi ALUR : identité des parties, description du logement (surface Boutin, nombre de pièces, annexes), conditions financières (loyer HC, charges, dépôt de garantie), durée et dates, et que les diagnostics obligatoires (DPE, CREP, ERP, électricité, gaz, bruit) sont listés en annexe. Vérifier qu'aucune clause interdite n'est présente (prélèvement auto imposé, assurance imposée, pénalités abusives, etc.).
Vérifier dans le code (models/Application.js, Property.requiredDocuments, app/apply/[id]/ApplyClient.tsx) que le dossier candidat ne demande AUCUN document interdit par le décret du 5 novembre 2015 : pas de photo d'identité séparée, pas de carte Vitale, pas de relevé bancaire, pas d'attestation bancaire, pas de casier judiciaire, pas de dossier médical, pas de contrat de mariage. Si un document interdit est trouvé, le supprimer du code et de l'interface.
Ajouter un monitoring basique : installer winston, configurer les logs en JSON structuré (timestamp, level, message, requestId, userId), créer un endpoint GET /health qui retourne le status de l'app et de la connexion MongoDB, ajouter un middleware qui log chaque requête entrante (method, path, statusCode, duration).
