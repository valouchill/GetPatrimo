Refactorer ApplyClient.tsx (7898 lignes) en suivant la méthode progressive : d'abord extraire les hooks custom (useDocumentUpload, useApplicationScoring, useGuarantee) dans app/apply/[id]/hooks/, puis extraire les composants (DocumentUploadSection, ScoringSection, GuaranteeForm, ProfileStep, RecapStep, ProgressBar) dans app/apply/[id]/components/. Le fichier parent doit faire moins de 300 lignes. Vérifier que TypeScript compile sans erreur après chaque extraction.
Refactorer OwnerDashboardClient.tsx (1411 lignes) : extraire PropertyGrid, CandidateList, StatsBar, AlertBanner dans des sous-composants dans app/(platform)/dashboard/owner/components/. Créer les hooks useOwnerProperties et useOwnerCandidates. Le fichier parent doit orchestrer les composants et faire moins de 200 lignes.
Refactorer BailInstant.tsx (1177 lignes) : séparer en LeaseForm (formulaire de bail), GuarantorSelector (choix garant), ClausesEditor (édition des clauses), PDFPreview (prévisualisation du PDF). Extraire les hooks de logique métier. Chaque composant < 300 lignes.
Refactorer LeaseWizard.tsx (1052 lignes) : créer un composant par step du wizard (StepLocataire, StepBien, StepConditions, StepDiagnostics, StepRecap) et un WizardShell qui gère la navigation entre steps. Chaque step < 200 lignes, le shell < 150 lignes.
Ajouter les attributs d'accessibilité de base dans tous les composants interactifs : aria-label sur tous les boutons icône (sans texte visible), role='dialog' + aria-modal sur les modals, role='tablist' + role='tab' sur les onglets, aria-required sur les champs obligatoires, aria-invalid sur les champs en erreur. Ajouter la navigation clavier (Tab + Enter/Space) sur les composants custom clickables. Tester avec un screen reader basique.
Dédupliquer les patterns de code frontend : identifier et extraire les composants dupliqués (Avatar, StatusBadge, LoadingSpinner, EmptyState) dans app/components/shared/. Identifier les patterns de fetch API dupliqués et les remplacer par les hooks custom créés en phase 3. Objectif : réduire la duplication de 18% à moins de 5%.
