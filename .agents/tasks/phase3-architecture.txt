Supprimer tous les fichiers .bak et .BROKEN du repo (202 fichiers). Ajouter *.bak et *.BROKEN dans .gitignore. Vérifier qu'aucun fichier .bak n'est importé ou référencé dans le code actif avant de supprimer. Faire un commit dédié pour ce nettoyage.
Inventorier toutes les routes définies dans server.js (Express) et toutes les routes dans app/api/ (Next.js API Routes). Identifier les doublons. Pour chaque route dupliquée, migrer la logique Express vers l'API Route Next.js et supprimer la route Express. Documenter dans un fichier MIGRATION-ROUTES.md les routes migrées et celles restantes.
Créer les hooks custom réutilisables dans app/hooks/ : useFetch (GET avec cache, loading, error), useAsync (exécution d'action async avec loading/error), useForm (gestion de formulaire avec validation), useDebounce (debounce d'une valeur). Chaque hook doit être typé en TypeScript, testé, et documenté avec un commentaire JSDoc. Remplacer au moins 3 utilisations de fetch dupliquées dans le code par useFetch.
Créer un système de notifications toast pour remplacer les 6 appels alert() : créer un NotificationProvider (React Context), un hook useNotification avec les méthodes success/error/warning/info, et un composant ToastContainer qui affiche les notifications en bas à droite avec animation. Remplacer tous les alert() existants par useNotification.
Implémenter la validation de formulaire avec Zod côté client : installer zod et @hookform/resolvers, créer un hook useZodForm qui combine react-hook-form + zodResolver. Créer les schémas Zod pour le formulaire de candidature (ApplySchema) et le formulaire de bail (LeaseSchema). Connecter useZodForm dans ApplyClient.tsx et LeaseWizard.tsx.
